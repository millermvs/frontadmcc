/**
 * ASSOCIADOS — Componente stub (Bloco 1)
 *
 * Estrutura mínima criada para satisfazer a rota /pages/associados
 * e permitir que o ng build compile sem erros.
 *
 * A implementação completa acontece no Bloco 2 (listagem paginada,
 * KPIs, filtros e tabela) e blocos seguintes.
 */
import { Component } from '@angular/core';

@Component({
  selector: 'app-associados',
  standalone: true,
  templateUrl: './associados.html',
  styleUrl: './associados.css',
})
export class Associados {}
