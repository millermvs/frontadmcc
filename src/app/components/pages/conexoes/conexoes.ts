import { Component } from '@angular/core';

/**
 * CONEXOES
 *
 * Página do módulo de Conexões (Fase 4 — Operacional).
 *
 * ── Estado atual: Parte 1 — Fundação ──────────────────────────
 * Componente vazio. Serve apenas para a rota /pages/conexoes
 * compilar sem erro. O conteúdo real será adicionado nas Partes
 * 2 e 3 do plano de implementação (abas Geradas e Recebidas).
 */
@Component({
  selector: 'app-conexoes',
  standalone: true,
  imports: [],
  templateUrl: './conexoes.html',
  styleUrl: './conexoes.css',
})
export class Conexoes {}
